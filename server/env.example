MONGODB_URI=mongodb+srv://demian:demian123@cluster0.y4swwy8.mongodb.net/?appName=Cluster0
MONGODB_DB=complejo_futbol_app
PORT=4000
# Clave secreta para firmar JWT (usada tanto por clientes como por administradores)
JWT_SECRET=cambia-esta-clave-en-produccion
# Lista de orígenes permitidos separados por coma, por ejemplo:
# ALLOWED_ORIGINS=http://localhost:8100,http://localhost:4200

# Config SMTP (requerido para enviar QRs por email)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_correo
# SMTP_PASSWORD=contraseña_o_token
# SMTP_FROM=La Sexta <no-reply@lasexta.com>

